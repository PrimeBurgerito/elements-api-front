#!/usr/bin/env groovy

pipeline {
    agent {
        docker {
            image 'node:latest'
            args '-v /var/www/elements/html:/elements-webapp/build -u root'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                dir('elements-webapp') {
                    sh 'npm ci'
                    sh 'npm run build'
                }
            }
        }
    }
}
